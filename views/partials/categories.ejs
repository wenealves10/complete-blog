<div class="nav-scroller py-1 mb-2">
    <nav class="nav d-flex justify-content-between">
        <% categories.forEach(category =>{%>
            <a class="p-2 text-muted" href="/categories/<%= category.slug %>"><%= category.title %></a>
        <% }) %>
    </nav>
</div>